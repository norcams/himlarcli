---
desc:   'Save state to a local sqlite db'
actions:
  save:     'Save the current state for a resource'
  compare:  'Compare saved and current state of a resource'
  purge:    'Remove saved stats for a resource'
  replace:  'Replace the current state with the one saved'

opt_args:
  '--region':
    'dest':     'region'
    'help':     'override current region'
  '--domain':
    'dest':     'domain'
    'help':     'openstack domain to use'
    'default':  'Dataporten'
  'resource':
    'metavar':  'resource'
    'help':     'resource name'
    'choices':  ['keypair', 'quota', 'instance']
